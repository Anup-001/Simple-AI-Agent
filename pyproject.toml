[project]
name = "Simple-AI-Agent"
version = "0.1.0"
description = "A simple CLI based agent "
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "langchain>=1.0.8",
    "langchain-openai>=1.0.3",
    "langgraph>=1.0.3",
    "python-dotenv>=1.2.1",
]
